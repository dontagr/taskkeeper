# SDD шаблон спеки (Go)

Этот шаблон — «контракт» между вами, ИИ и кодом. ИИ может помогать заполнять пункты, но **утверждаете spec вы**.

---

## 0) Заголовок

**Название:**  
**Дата:**  
**Автор:**  
**Статус:** draft / approved / implemented  

---

## 1) Контекст

- **Где живёт код:** (repo / модуль / сервис)  
- **Что болит:** (1–3 предложения)  
- **Кого/что затрагивает:** (пользователи, внутренние сервисы, QA)  

---

## 2) Цель

Сделать \(X\), чтобы получить \(Y\).

**Definition of Done (DoD):**

- [ ] Добавлен функционал \(…\)
- [ ] Тесты: unit \(…\), integration \(…\) (если нужно)
- [ ] `go test ./...` проходит локально
- [ ] (опционально) линтер/форматтер: `gofmt` / `go vet` / `golangci-lint` (если используется)
- [ ] Документация/README обновлены (если нужно)

---

## 3) Не-цели (важно!)

- Не делаем \(…\)
- Не меняем \(…\)
- Не оптимизируем \(…\)

---

## 4) Требования (functional)

Список требований в виде проверяемых пунктов.

- R1: …
- R2: …

---

## 5) API / интерфейсы

### 5.1 HTTP (если есть)

- **Base path:** `/api/...`
- **Endpoints:**
  - `GET /...` — вход/выход, коды ответов
  - `POST /...` — вход/выход, коды ответов

**Коды и ошибки:**

- 200/201: …
- 400: …
- 404: …
- 409: …
- 500: …

### 5.2 Внутренние интерфейсы (если есть)

Какие интерфейсы/пакеты должны появиться или измениться.

---

## 6) Модель данных

### 6.1 Структуры Go

```go
type Example struct {
    // поля
}
```

### 6.2 Хранилище (если есть)

- Таблицы/коллекции/ключи
- Миграции (да/нет)

---

## 7) Поведение и edge cases

- Пустой ввод
- Невалидный ввод
- Повторный запрос (идемпотентность)
- Конкурентные запросы
- Большие размеры (лимиты)

---

## 8) Набросок реализации (technical design, 5–15 строк)

- Пакеты/слои: `cmd/`, `internal/`, `pkg/`
- Где будет HTTP хендлер
- Где бизнес-логика
- Где валидация
- Где хранилище (in-memory/DB) и почему

---

## 9) План тестирования

### 9.1 Unit

- Что тестируем (функции/методы)
- Табличные тесты (table-driven) — да/нет

### 9.2 Integration / E2E (если есть)

- Как поднимаем окружение (или почему не нужно)
- Какие сценарии критичные

---

## 10) Наблюдаемость и эксплуатация (если релевантно)

- Логи: какие события логируем, что НЕ логируем (PII!)
- Метрики: что измеряем
- Фича-флаг (если нужен)

---

## 11) Риски и откат

- Что может пойти не так
- Как откатить (rollback plan)

---

## 12) Вопросы / неизвестные

- Q1: …
- Q2: …

---

## Мини-промпт для ИИ (вставить в чат одним куском)

Скопируйте, заполните плейсхолдеры и используйте, когда просите ИИ помочь **по утверждённой спеки**.

```
Ты мой помощник по Go. Работай строго в рамках спеки ниже.

Ограничения:
- Не меняй публичные контракты без явного пункта в spec.
- Предлагай маленькие диффы и этапы.
- Если есть неясность — сформулируй вопросы, не выдумывай.
- Без секретов/PII.

Spec:
<вставь сюда пункты 1–11>
```

